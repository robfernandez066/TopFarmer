TASK-015: Correct the portrait viewport and re-verify FarmPlot states
Status: ready
Complexity: complex
Files: inspect and modify res://project.godot only for the authorized display settings; preserve res://scenes/farm/farm_plot.tscn, res://scripts/farm/farm_plot.gd and its UID, res://tests/farm_plot_visual_state_test.gd and its UID, and res://tests/visual/farm_plot_states_preview.tscn from commit 21dc94a; create res://tests/portrait_display_contract_test.gd and its Godot UID sidecar
Acceptance:
- At task start, inspect the existing uncommitted `project.godot` diff and report its exact changed setting keys. Do not discard it blindly: retain any change that exactly matches this task's authorized values, replace a conflicting value with the authorized value, and do not retain or introduce any unrelated project-setting change
- Modify `project.godot` only as needed to set `display/window/size/viewport_width` to integer `540`, `display/window/size/viewport_height` to integer `960`, `display/window/size/window_width_override` to integer `540`, `display/window/size/window_height_override` to integer `960`, `display/window/stretch/mode` to string `canvas_items`, `display/window/stretch/aspect` to string `expand`, and `display/window/handheld/orientation` to integer `1` for portrait
- Preserve the pinned Godot 4.7.1 Compatibility renderer, application name, main scene, Balance autoload, input settings, and every project setting outside those seven display keys; do not add an override file, runtime window-resize script, editor preference, plugin, export preset, or platform-specific workaround
- Preserve every TASK-015 scene and script from commit `21dc94a` without changing node structure, plot transforms, textures, state behavior, preview background, or preview plot positions; after the correction, `git diff 21dc94a --` for those existing TASK-015 files must be empty
- Create `portrait_display_contract_test.gd` as a headless first-party test that verifies all seven authorized ProjectSettings values with exact types and values, verifies the root viewport reports 540x960 at startup, rejects width/height reversal, and ends with exactly `PASS: portrait display contract`
- The Coder report lists the seven final display values, shows that no unrelated `project.godot` key changed, confirms the pre-existing uncommitted config edit was audited and either retained or corrected, and confirms all original TASK-015 component files remain unchanged from commit `21dc94a`
- When `farm_plot_states_preview.tscn` runs in a floating fixed-size game window, the drawable client area is 540 pixels wide by 960 pixels high in portrait orientation; the repeated meadow covers the entire drawable area with no gray region on the right or bottom
- The preview still shows three separate centered plots at ground points `(270, 290)`, `(270, 570)`, and `(270, 850)`: empty at top, growing in the middle, and ready at bottom; the bottom plot and mature crop are fully visible with no clipping, and the already-approved plot/crop alignment remains unchanged
- Do not modify approved art, import sidecars, metadata, balance data, audio, localization, the main scene, main script, farm logic, camera behavior, UI, or any existing test other than running it
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --script res://tests/portrait_display_contract_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_visual_state_test.gd`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully; the new test ends with `PASS: portrait display contract`, the existing plot test ends with `PASS: farm plot visual states`, normal boot prints `TOPFARMER_BOOT_OK`, and the balance test ends with `PASS: all 7 balance files loaded; bad test data was rejected`
Human check:
- Close any currently running preview. In Godot's Game view menu, choose `Make Window Floating on Next Play` and choose the `Fixed Size` window-sizing option so an editor stretch preference cannot force a landscape shape
- Open `tests/visual/farm_plot_states_preview.tscn` and press F6. The new game window's inside area should be tall and narrow in a 540-by-960 portrait shape, not wide like a landscape screen
- The meadow should cover the entire inside of the window with no large gray area on the right or bottom, and all three plots should be fully visible from top to bottom; the lowest golden ready crop must not be clipped
- Confirm the plots themselves remain straight and aligned as before, with the young and ready plants meeting the soil naturally. Nothing should respond when clicked or tapped; close the preview with F8
- Review the Coder report and confirm the display test finished with `PASS: portrait display contract` and that only the seven listed display settings changed in `project.godot`
Depends on: TASK-014 and the initial TASK-015 implementation in commit 21dc94a
Notes: see DEC-002, DEC-017, DEC-044, and DEC-045. The first TASK-015 Human check rejected the preview because it opened landscape, rendered meadow only across the left 540 pixels, left a large gray region on the right, and clipped the bottom ready plot. Preserve the correct plot component and fix only the project-wide 540x960 portrait display contract. Do not begin interaction, timers, camera work, farm layout, HUD, audio, vibration, save data, or TASK-016.
