TASK-016: Generate a TF-ART-v1.2 mature Sunwheat perspective candidate
Status: ready
Complexity: routine
Files: create res://assets/source/ai/candidates/sunwheat_crop_ready_angle_candidate_512.png, its Godot import sidecar, and res://assets/source/ai/candidates/sunwheat_crop_ready_angle_candidate_512.metadata.md
Acceptance:
- Preserve every existing source asset, runtime asset, scene, script, test, project setting, balance file, audio file, and previous TASK-015 commit unchanged; this task creates one source-only color candidate and metadata and does not replace the crop used by the game
- Use the built-in image-generation workflow and locked `TF-ART-v1.2` recipe with this exact positive prompt: `TopFarmer game sprite of a single mature Sunwheat crop cluster with five to seven plump sun-gold grain heads, healthy green upright stalks and leaves, one subordinate dusk-purple grain head, and all stems visibly converging into one planted base, harvest-ready high-angle perspective calibration candidate, 2D hand-painted fantasy cartoon, cozy rural warmth with a restrained mysterious undertone, top-down orthogonal square ground plane with a high-angle front-elevated farm-game sprite presentation for subjects with height, vertical forms rising upward on screen from a bottom-center ground contact, clear readable front planes with modest visible top surfaces, near elements lower and larger and rear elements higher and smaller, readable mobile silhouette, TopFarmer locked v1 palette, key light from upper left at 35 degrees, subtle dark-plum outline, tile-filling opaque square canvas for flat terrain or isolated on transparent background for object sprites, centered on a 512 by 512 canvas, no text`
- Use the locked negative prompt without additions or removals: `photorealism, 3D render, anime, pixel art, horror, gore, neon, glossy plastic, isometric projection, diamond tile, 30-degree isometric axes, diagonal grid movement, fisheye, straight-down view for upright subjects, flat radial rosette, low eye-level front view, pure side view, inconsistent perspective, multiple objects, scenery, background, baked cast shadow, cropped edges, text, watermark`
- The candidate reads as one planted crop viewed from above and in front: its stems converge at a single visible base around source point `(256, 465)` and rise upward on the canvas; lower foreground stalks and leaves are larger and show readable front planes; overlapping central heads establish depth; smaller rear heads sit higher and show only modest top surfaces
- Reject any candidate that reads as a flat circular rosette, overhead flower, evenly spread starburst, eye-level bouquet, pure side view, isometric or diamond arrangement, disconnected pile of grain heads, or several separate plants. Do not compensate for a failed perspective with scene offsets
- Retain the Sunwheat identity and locked visual language: five to seven plump sun-gold heads, healthy leaf and moss greens, exactly one subordinate dusk-purple head, subtle outline derived from `#3E3840`, upper-left form light, cozy rural warmth, restrained mystery, and a readable silhouette at mobile size. Do not add a face, pot, soil patch, tool, scenery, glow halo, writing, watermark, background, or baked shadow
- Existing crop art may be supplied only as a species, palette, anatomy, material, and lighting reference; do not preserve or trace its rejected radial composition. Existing approved upright art may be supplied only as a TopFarmer rendering and angle reference without copying its shapes or content
- Prepare an accepted generation only by lossless background removal when needed, centered square cropping or transparent padding without stretching, one high-quality resize to exact 512x512, whole-image translation to place the planted base at `(256, 465)`, and clearing RGB where alpha is zero; do not hand-paint, redraw, recolor, mirror, warp, sharpen, or combine candidates
- The committed candidate is an exact 512x512 lossless straight-alpha RGBA PNG with transparent corners, safe margins on every side, zero hidden RGB, no clipped painted pixel, no separate or baked shadow, and an optically centered planted base rather than a whole-image luminance-centroid alignment
- Create complete `TF-ART-v1.2` metadata recording the exact prompts, available tool/model information, UTC date, candidate identifier, requested and delivered sizes, supplied references and their limited purposes, rejected attempts and reasons, permitted preparation, source hash, mode, alpha extrema and bounds, margins, hidden-RGB result, planted-base point, perspective review, palette review, reviewer, date, and source-only disposition
- The Coder report provides two temporary uncommitted diagnostics: a normal-size 512x512 view on a neutral background and a 540x960 portrait preview placing one exact 256x256 Lanczos reduction of the candidate on the approved empty soil plot without changing the game scene. The candidate must remain readable as front-elevated and naturally planted in both views; diagnostics remain outside the repository
- Godot imports the candidate with lossless texture settings, mipmaps disabled, no size limit, no premultiplied alpha, and no normal-map, HDR, or channel-remap alteration; no game scene, script, test, or resource references the candidate
- The Coder report includes the exact prompts, PNG dimensions and alpha checks, planted-base position, source hash, repository-scope proof, both diagnostics, and perspective rejection-checklist result
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --script res://tests/portrait_display_contract_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_visual_state_test.gd`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully with their existing required PASS and boot lines
Human check:
- Open the Coder report's 512x512 candidate view and 540x960 portrait preview at normal size
- Confirm the crop has the same kind of angle relationship as the approved Stardew Valley reference: it grows upward from one planted base, you can clearly see the fronts of its stalks and leaves, and you can see only a smaller amount of the tops of the rear grain heads
- Reject it if it looks like a flower or starburst seen straight down, a bouquet viewed straight from the front, several separate plants, or an isometric diamond arrangement
- In the portrait preview, confirm the plant looks naturally rooted near the center of the brown plot. Ignore the lack of a finished shadow; this task approves only the crop's angle and base position
- Confirm the Coder report says the candidate is source-only and no existing image or game file changed
Depends on: TASK-015 and DEC-051
Notes: see DEC-047 through DEC-052. This is the first art-generation task under `TF-ART-v1.2`. Human angle approval is required before any shadow is authored, runtime crop is replaced, scene alignment is revised, or gameplay work resumes.
