TASK-023: Make the four-plot real-time farm the main game screen
Status: ready
Complexity: complex
Files: create res://scenes/farm/farm_world.tscn, res://scripts/farm/farm_world.gd, and its Godot UID sidecar; modify res://scenes/main.tscn; create res://tests/farm_world_runtime_test.gd and its Godot UID sidecar
Acceptance:
- Create `class_name FarmWorld extends Node2D` and a reusable `farm_world.tscn` with exactly these authored children: a full-viewport `TextureRect` named `MeadowBackground`, a `Node2D` named `PlotLayer`, and one `Node` named `InteractionController` using the approved FarmPlotInteractionController script
- `MeadowBackground` fills logical rect `(0, 0, 540, 960)`, uses only `res://assets/sprites/terrain/meadow_grass_tile_default_128.png`, tiles rather than stretches it with texture repeat enabled, draws behind plots, and ignores mouse/touch input
- On `_ready`, FarmWorld reads `STARTING_PLOTS` only through the Balance autoload. Require a finite positive whole number no greater than the four authored Phase 2 slots; invalid, fractional, zero, negative, or over-capacity data fails loudly before any plot is registered and never substitutes a hardcoded count
- Use the four ordered bottom-center ground-contact slots `(140, 390)`, `(400, 390)`, `(140, 710)`, and `(400, 710)`. Instantiate exactly the Balance-provided number of `res://scenes/farm/farm_plot.tscn` children under `PlotLayer`, in slot order, at scale `(1, 1)` and rotation `0`, named `Plot01` through `Plot04`; with the current Balance value all four plots exist in a 2x2 layout and remain fully inside the 540x960 playfield
- Own exactly one SessionUtcClock instance for the FarmWorld lifetime. Start it once, fail loudly without partial controller configuration if startup fails, configure the existing InteractionController with `Callable(clock, "now_utc")` and selected crop ID `sunwheat`, then register each created plot exactly once
- Normal runtime contains no fake clock, Space shortcut, accelerated timer, hardcoded growth duration, system-clock polling outside SessionUtcClock, or fallback UTC. Sunwheat becomes READY only when the one session clock reaches the CSV-derived `ready_at_utc`
- All four plots start EMPTY and behave independently. Tapping any EMPTY plot plants Sunwheat; GROWING taps do not mutate it; reaching its own deadline changes only that plot to READY; tapping READY harvests only that plot to EMPTY; the next tap on that EMPTY plot smart-replants its remembered Sunwheat. Process updates never auto-replant
- Keep one soft click and one held press response per accepted gesture. The FarmWorld and controller add no duplicate sound or press animation. Simultaneous crops may share the one clock value but retain distinct start and ready timestamps according to their actual planting times
- Instance exactly one `farm_world.tscn` as a child of the existing Main root in `main.tscn`. Preserve the Main root, `scripts/main.gd`, `TOPFARMER_BOOT_OK` diagnostic, project run target, 540x960 portrait display contract, Balance autoload, renderer, and every project setting unchanged
- Provide an overridable internal clock-construction method in FarmWorld so the first-party test can supply a deterministic SessionUtcClock subclass. Do not expose a public clock replacement, fake-time action, or debug shortcut in the runtime scene
- The headless test loads the real Main and FarmWorld scenes and proves: exact node names and types; tiled noninteractive meadow resource; Balance-derived plot count; exact plot names, parents, slot positions, transforms, and distinct identities; one clock instance and one controller registration per plot; Main contains exactly one FarmWorld; the boot diagnostic remains; and no plot extends outside the logical playfield
- With a deterministic clock subclass, the test proves two plots planted at different supplied times have distinct absolute timestamps, mature independently at their exact CSV-derived deadlines, READY waits without auto-harvest or auto-replant, harvesting one does not alter another, and one later tap smart-replants only the harvested plot
- The test also proves invalid, fractional, and over-capacity `STARTING_PLOTS` paths fail without partial registration through a test-controlled Balance response or isolated validation helper; production code never edits Balance or substitutes a count
- Do not add inventory, seed costs, yields, Gold, XP, Starlight, rewards, selection UI, HUD, floating numbers, particles, vibration, save data, offline reconciliation, pause/resume policy, camera pan/zoom, buildings, Android APIs, or final crop-art replacement in this task
- Every existing asset, balance file, FarmPlot file, controller file, clock file, localization file, test, and project setting remains byte-for-byte unchanged; only the listed files and append-only Coder report change
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --script res://tests/portrait_display_contract_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_visual_state_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_crop_lifecycle_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_touch_feedback_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_interaction_controller_test.gd`, `godot_console --headless --path . --script res://tests/session_utc_clock_test.gd`, `godot_console --headless --path . --script res://tests/farm_world_runtime_test.gd`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully; the new test prints exactly one final `FARM_WORLD_RUNTIME_TEST_PASS` line and normal boot still prints `TOPFARMER_BOOT_OK`
Human check:
- Press F6 on `scenes/farm/farm_world.tscn`, or press F5 to run the game from Main
- Confirm the portrait meadow shows four complete brown plots in a clear 2x2 layout, with nothing clipped and no gray area
- Click one empty plot. It should make one soft click and immediately show the small green GROWING crop; clicking a different plot should plant that plot independently
- Leave the first crop running for the current Sunwheat CSV duration, about two minutes. It should become the mature READY placeholder by itself while the game remains open; it must not harvest or replant itself
- Click the READY plot once to harvest it to empty soil, then click it once more to replant Sunwheat. The other plots must not change from either click
- Confirm the Coder report says the real runtime has no Space shortcut or fake clock, the new test printed `FARM_WORLD_RUNTIME_TEST_PASS`, and normal boot still printed `TOPFARMER_BOOT_OK`
Depends on: TASK-022, TASK-021, and the Balance `STARTING_PLOTS` global
Notes: see DEC-002, DEC-003, DEC-058 through DEC-066. This is the first real Main-scene farm slice for A1, A2, B1, B2, C1, C3, K1, and K2. Inventory/rewards, richer action feedback, camera gestures, save/offline behavior, Android deployment, and final crop perspective remain later tasks.
