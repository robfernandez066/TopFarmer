TASK-015: Correct FarmPlot crop visual centering and re-verify
Status: ready
Complexity: complex
Files: inspect and restore res://project.godot to the committed portrait contract from commit 0a854a1 if the editor introduced a non-semantic reorder; modify res://scripts/farm/farm_plot.gd and res://tests/farm_plot_visual_state_test.gd; preserve res://scenes/farm/farm_plot.tscn, res://tests/portrait_display_contract_test.gd, and res://tests/visual/farm_plot_states_preview.tscn unless a generated UID reference alone requires normalization
Acceptance:
- At task start, inspect the current uncommitted `project.godot` diff. The seven portrait settings committed in `0a854a1` must remain exact. If the diff only reorders the unchanged `application/run/main_scene` entry as previously observed, restore the committed textual form and exclude `project.godot` from the corrective commit; if any setting value differs, report `needs-decision` instead of silently keeping or discarding it
- Preserve every approved PNG byte-for-byte. In particular, growing runtime crop/shadow hashes remain `05889B781E5F81B57EC989CAD7748136B4087B1F54086F1435F7480257E12819` and `3AF242C4CBEE7F6C1E7C111287D804C10614B17B6E9ECABECC5045F33AB33B8B`; ready runtime crop/shadow hashes remain `711D3F1CBC73B9B6031795535CBAF723C6832C03A44EB1BE0DDF45F9156019F4` and `311BDB3904A9B0BDED80ECEECA80939CC846DF32CF62B30BA72D18E40F6CA633`
- Keep `PlotBase` fixed at position `(-128, -232.5)`, keep every `FarmPlot` root and preview ground point unchanged, and keep the crop color and shadow y-coordinate exactly `-232.5` in every state; do not round or otherwise alter the vertical soil-contact alignment
- In `EMPTY`, keep both hidden crop nodes at neutral position `(-128, -232.5)`. In `GROWING`, set both `CropShadow` and `CropColor` to exactly `(-127, -232.5)`, shifting the pair right by 1 pixel relative to the plot base. In `READY`, set both to exactly `(-140, -232.5)`, shifting the pair left by 12 pixels relative to the plot base
- Apply each state's shadow and color textures, visibility, and identical pair position as one state update. Repeated `EMPTY -> GROWING -> READY -> EMPTY` cycles must use absolute positions and never accumulate movement, separate the shadow from its crop, move the base, or change any node's scale, rotation, skew, z-order, or parent
- The Coder report independently measures alpha-weighted and alpha-plus-luminance-weighted x centroids for the plot and both crop color sprites before and after offsets. Using the committed decoded PNGs, the report should reproduce approximately plot visual x=`127.67`, growing visual x=`126.69`, and ready visual x=`140.06` before offsets, then show growing near `127.69` and ready near `128.06` after the authorized shifts
- Update `farm_plot_visual_state_test.gd` to assert the exact state-specific positions of both crop nodes, identical shadow/color coordinates, unchanged base position, exact texture pairing and visibility, fixed node identity, unchanged y-coordinate, and no cumulative drift across at least two complete state cycles; it must still end with exactly `PASS: farm plot visual states`
- Preserve the 540x960 portrait display contract and the preview's full meadow background and three root ground points `(270, 290)`, `(270, 570)`, and `(270, 850)`. In the corrected preview, the growing crop's painted center lands near screen x=`269.69` and the ready crop's painted center near x=`270.06`, visually matching the soil plot center near x=`269.67`
- The Coder report includes a fresh 540x960 floating fixed-size screenshot showing all three plots. The READY silhouette must no longer look pulled to the right, both planted states must read as visually centered over their soil patches, and each shifted shadow must remain attached beneath its matching lowest leaves
- Do not modify the plot asset, crop or shadow assets, preview root positions, viewport settings, main scene, balance data, source art, metadata, import sidecars, audio, localization, camera, UI, input, timers, or gameplay behavior
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --script res://tests/portrait_display_contract_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_visual_state_test.gd`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully; the display test ends with `PASS: portrait display contract`, the plot test ends with `PASS: farm plot visual states`, normal boot prints `TOPFARMER_BOOT_OK`, and the balance test ends with `PASS: all 7 balance files loaded; bad test data was rejected`
Human check:
- Run `tests/visual/farm_plot_states_preview.tscn` with F6 in a floating fixed-size game window. It should remain a fully filled 540-by-960 portrait meadow with all three plots visible
- Imagine a straight vertical line through the middle of each brown soil patch. The young green plant and the full golden ready plant should now look balanced around that line; especially reject the result if the READY crop still looks noticeably pulled toward the right edge
- Compare the visible soil space on the left and right of each planted crop. It does not need to be pixel-identical, but neither crop's painted weight should make one side feel crowded while the opposite side feels empty
- Confirm each crop still meets the same vertical place on the soil and its shadow remains directly attached beneath the lowest leaves; reject any horizontal correction that creates a detached shadow, hover gap, or vertical jump
- Nothing should respond when clicked or tapped. Review the Coder report and confirm both automated tests finish with `PASS: portrait display contract` and `PASS: farm plot visual states`
Depends on: TASK-014 and TASK-015 commits 21dc94a and 0a854a1
Notes: see DEC-002, DEC-017, DEC-044, DEC-045, and DEC-046. The viewport correction passes, but the second Human check rejects visual centering: mathematical root alignment is insufficient when a painted silhouette is asymmetrically weighted. Supersede DEC-044 only for crop-layer x offsets; preserve plot roots, base position, vertical soil contact, approved assets, and portrait settings. Do not begin TASK-016 or any gameplay work.
