TASK-020: Add direct-touch FarmPlot activation feedback
Status: ready
Complexity: complex
Files: create res://assets/audio/sfx/ui_tap_soft.wav and its Godot import sidecar; modify res://scenes/farm/farm_plot.tscn and res://scripts/farm/farm_plot.gd; create res://tests/farm_plot_touch_feedback_test.gd and its Godot UID sidecar
Acceptance:
- Copy the approved source `res://assets/audio/reference/ui_tap_soft_kenney_click1.wav` byte-for-byte to `res://assets/audio/sfx/ui_tap_soft.wav`; both files remain exactly 16,596 bytes with SHA-256 `359C8C98ECD48368F45B84D3A9B9D7E68CF3AC2FEFCDBF32367B8482DA76C9C7`, and the existing CC0 license record remains unchanged and applicable
- Godot imports the runtime WAV losslessly as a non-looping audio stream without format conversion, trimming, normalization, pitch change, or channel change; do not modify either source reference file, its import sidecar, or its license record
- Add one input-pickable `Area2D` child to FarmPlot with one `CollisionShape2D` using a `RectangleShape2D` of exact size `(232, 216)` at local position `(0, -112)`. This covers the visible soil while leaving the FarmPlot origin at its bottom-center ground contact
- Add one non-positional `AudioStreamPlayer` child using only `res://assets/audio/sfx/ui_tap_soft.wav`, with autoplay and looping disabled and default volume and pitch unchanged
- Declare one `activated` signal. One accepted primary gesture emits `activated` exactly once when that gesture releases over the same FarmPlot; it never emits on press, motion, cancellation, release outside, right or middle mouse input, mouse wheel, key input, a second simultaneous finger, or synthetic mouse events generated from an already handled touch
- Native `InputEventScreenTouch` is authoritative. Track one active finger index from press through release, ignore all other fingers until it ends, mark accepted touch events handled so emulated mouse cannot duplicate them, and support left-mouse press/release only as an editor/desktop testing fallback when no touch gesture is active
- Accepted press begins immediate feedback before release: play the runtime soft-click exactly once and set the FarmPlot scale to its captured pre-press scale multiplied uniformly by `0.97`, preserving the ground-contact origin and rotation. Accepted release or cancellation restores the captured scale exactly; repeated gestures never accumulate scale drift
- If the pointer or finger leaves the hit area before release, cancel that gesture, restore scale exactly, emit no activation, and do not play a second sound. Losing focus, leaving the scene tree, becoming hidden, or being freed while pressed also restores or safely discards the captured state without an activation
- Input feedback never calls `plant_crop`, `update_growth`, `harvest_crop`, or `replant_last_crop` and never changes lifecycle state, crop IDs, timestamps, remembered crop, visual state, texture, layer position, or z order. All EMPTY, GROWING, READY, lifecycle, and placeholder behavior remains exact
- Do not add crop selection, planting, harvesting, clock access, frame-based growth, inventory, costs, yields, rewards, UI, user-facing text, particles, vibration, camera behavior, persistence, or project settings in this task
- The new headless test verifies exact hit-area node type, size, position, input-pickable state, audio node type and exact stream path/hash, nonlooping configuration, signal existence, one activation per valid touch and left-click, touch-over-mouse deduplication, ignored secondary/right/middle inputs, release-outside cancellation, lifecycle immutability in all three states, exact press scale and restoration, and at least ten repeated gestures without transform or node drift
- The existing visual-state and lifecycle tests remain unchanged and pass. `project.godot`, every PNG, every balance file, every source audio file, every license record, the visual preview scene, and every file not listed above remain byte-for-byte unchanged except the append-only Coder report
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --script res://tests/portrait_display_contract_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_visual_state_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_crop_lifecycle_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_touch_feedback_test.gd`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully; the new test prints exactly one final `FARM_PLOT_TOUCH_FEEDBACK_TEST_PASS` line
Human check:
- In Godot, open `tests/visual/farm_plot_states_preview.tscn` and press F6
- Press and hold each brown plot once with the left mouse. Each entire plot should become slightly smaller immediately, remain anchored at the same planted ground point, play one soft click, and return to exactly its original size when released
- Click repeatedly and confirm every click produces one sound and one press response, with no double sound, stuck-small plot, sideways jump, crop-state change, planting, or harvesting
- If touch testing is available on the current device, repeat with one finger and confirm it behaves the same; a second finger touching simultaneously must not trigger another activation
- Confirm the Coder report says the plot states and timestamps never changed and the test ended with `FARM_PLOT_TOUCH_FEEDBACK_TEST_PASS`
Depends on: TASK-019 and the Human-approved TASK-009 primary-touch SFX reference
Notes: see DEC-028, DEC-032, DEC-059, and DEC-060. This task establishes K1/H4 gesture and immediate-feedback plumbing only. The next task will connect the single `activated` signal to plant, harvest, and smart-replant actions with a caller-supplied UTC source.
