TASK-017: Export the temporary mature Sunwheat placeholder pair
Status: ready
Complexity: complex
Files: create res://assets/source/ai/candidates/sunwheat_shadow_ready_placeholder_v2_512.png, its Godot import sidecar, and res://assets/source/ai/candidates/sunwheat_shadow_ready_placeholder_v2_512.metadata.md; create res://assets/sprites/crops/sunwheat_crop_ready_placeholder_256.png and res://assets/sprites/crops/sunwheat_shadow_ready_placeholder_256.png with their Godot import sidecars
Acceptance:
- Preserve `sunwheat_crop_ready_angle_candidate_v2_512.png` byte-for-byte at SHA-256 `6AA30424C639B34B9ACC74509B6218DEB6522C6D80011F1D98A655923A2AEBD8`; preserve both rejected candidate histories and every existing source/runtime asset, scene, script, test, project setting, balance file, audio file, and localization file unchanged
- Treat the selected source only as a temporary Phase 2 gameplay placeholder under DEC-054. Do not call it approved final art, do not use it as a perspective reference for another asset, and do not remove `placeholder` from either runtime filename
- Create the exact 512x512 source shadow procedurally on a transparent RGBA canvas. Use neutral-cool RGB `(83, 86, 101)` wherever alpha is nonzero. Build a contact component from ellipse box `[174, 430, 338, 476]` filled to alpha `68` and a connected down-right extension from ellipse box `[212, 438, 380, 486]` filled to alpha `32`; apply Pillow 12.2.0 `ImageFilter.GaussianBlur(10)` to each alpha mask separately, merge them by per-pixel maximum, then clear RGB wherever final alpha is zero
- The source shadow remains one connected soft shape, overlaps the placeholder crop around its basal leaves, stays centered beneath the single ground contact `(256, 465)`, extends modestly down-right, has four transparent corners and zero hidden RGB, and does not read as a detached lobe, platform, puddle, halo, or broad floating oval
- Create `sunwheat_crop_ready_placeholder_256.png` from the unchanged 512x512 candidate with exactly one full-canvas Pillow 12.2.0 `Image.Resampling.LANCZOS` resize to 256x256 followed only by clearing RGB where alpha is zero; do not crop, stretch, sharpen, recolor, repaint, offset, or otherwise alter it
- Create `sunwheat_shadow_ready_placeholder_256.png` from the exact 512x512 placeholder shadow with exactly one full-canvas Pillow 12.2.0 `Image.Resampling.LANCZOS` resize to 256x256 followed only by clearing RGB where alpha is zero; do not add, reshape, or offset the runtime shadow
- Both runtime PNGs are exact 256x256 lossless straight-alpha RGBA files with four transparent corners, zero hidden RGB, no clipped painted pixel, and shared mapped ground contact `(128, 232.5)`; a fresh one-pass reconstruction from each 512x512 source is pixel-identical to its committed runtime file
- Create complete source-shadow metadata citing DEC-054 and DEC-055 and recording the selected crop source hash, placeholder-only disposition, deterministic ellipse boxes, colors, alpha fills, blur implementation and version, maximum merge, source and runtime hashes, dimensions, modes, alpha extrema and bounds, margins, connectedness, overlap, hidden-RGB results, import settings, reviewer, and date
- Godot imports all three new PNGs losslessly with mipmaps disabled, no size limit, no premultiplied alpha, and no normal-map, HDR, or channel-remap alteration
- No scene, script, test, project setting, resource, or autoload references either placeholder runtime PNG in this task; runtime integration requires a later PM task after Human review of the pair
- The Coder report provides one temporary uncommitted native 256x256 composite placing the placeholder shadow beneath the placeholder crop on the approved empty soil plot and meadow, plus a 540x960 portrait preview of that same composite. Both diagnostics remain outside the repository
- The Coder report includes source immutability and hash proof, exact runtime reconstruction checks, shadow construction and connectedness checks, crop-shadow overlap, PNG dimensions and alpha checks, import checks, repository-scope proof, both diagnostics, and all required Godot regression results
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --script res://tests/portrait_display_contract_test.gd`, `godot_console --headless --path . --script res://tests/farm_plot_visual_state_test.gd`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully with their existing required PASS and boot lines
Human check:
- Open the native-size composite and the portrait preview linked in the Coder report
- Confirm the crop has no magenta background and is the same selected plant from TASK-016, now resting on the brown soil with a small soft shadow attached beneath its lowest leaves
- Reject the pair if the shadow is detached, makes the plant float, forms a dark platform or puddle, or extends as a large oval far beyond the plant base
- Confirm the Coder report identifies the crop as a temporary placeholder and says the game still does not use either new runtime file
- This check does not approve the crop's final angle. The known front-facing angle remains deferred; approve only whether this temporary pair is clean and grounded enough for later gameplay integration
Depends on: TASK-016 and DEC-054
Notes: see DEC-047 through DEC-055. Final Sunwheat perspective work resumes after the functional Phase 2 plot loop exists and before Phase 2 exit approval. This task exports a reversible placeholder pair only; it does not modify gameplay or scene references.
