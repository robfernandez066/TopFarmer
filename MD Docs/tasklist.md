TASK-014: Generate the growing Sunwheat runtime pair
Status: ready
Complexity: complex
Files: create res://assets/source/ai/production/sunwheat_crop_growing_512.png and its Godot import sidecar; create res://assets/source/ai/production/sunwheat_shadow_growing_512.png and its Godot import sidecar; create res://assets/source/ai/production/sunwheat_crop_growing_512.metadata.md; create res://assets/sprites/crops/sunwheat_crop_growing_256.png and its Godot import sidecar; create res://assets/sprites/crops/sunwheat_shadow_growing_256.png and its Godot import sidecar
Acceptance:
- Use the built-in image-generation workflow and locked `TF-ART-v1.1` recipe to create exactly one growing `sunwheat` crop color sprite; use this exact positive prompt: `TopFarmer game sprite of sunwheat growing crop cluster with five short healthy green shoots, small folded leaves, one tiny closed pale-gold seed bud, and one subtly dulled dusk-tinted leaf, young growing state waiting to mature, 2D hand-painted fantasy cartoon, cozy rural warmth with a restrained mysterious undertone, mostly overhead top-down farm-game view with a slight downward angle, screen-aligned orthogonal ground plane, visible top surfaces and a small front face where the subject has height, readable mobile silhouette, TopFarmer locked v1 palette, key light from upper left at 35 degrees, subtle dark-plum outline, tile-filling opaque square canvas for terrain or isolated on transparent background for object sprites, centered on a 512 by 512 canvas, bottom-center ground contact aligned where applicable, no text`
- Use the locked negative prompt without additions or removals: `photorealism, 3D render, anime, pixel art, horror, gore, neon, glossy plastic, isometric projection, diamond tile, 30-degree isometric axes, diagonal grid movement, fisheye, front view, side view, inconsistent perspective, multiple objects, scenery, background, baked cast shadow, cropped edges, text, watermark`
- The approved mature Sunwheat source, corrected mature shadow, and empty tilled-soil plot may be supplied only as style, scale, palette, perspective, and alignment references; do not copy, edit, trace, or replace any existing asset
- The accepted crop reads immediately as the same Sunwheat species at an unfinished young stage: one compact low planted cluster dominated by green shoots and folded leaves, with one small closed pale-gold bud and one subordinate dusk-tinted leaf; it has no mature plump grain heads, harvest glow, flower, fruit, face, pot, soil patch, tool, text, watermark, scenery, background, or baked shadow
- Use leaf `#7FAE59` and moss `#5E7A4A` as the controlling colors, with restrained outline `#3E3840`, bud detail derived from sun `#E9C46A`, and exactly one quiet leaf accent derived from dusk `#705B8C`; value and saturation shifts may paint form, but the young crop must remain visibly greener and much less golden than the approved ready crop
- The crop uses the mostly overhead, slightly angled non-isometric view, visible leaf top surfaces, upper-left form light, subtle dark-plum outline, and a centered readable silhouette; it must not read as a front-facing bouquet, tall fan, side view, diamond arrangement, or miniature copy of the mature cluster
- Prepare the accepted generated art only by lossless chroma/background removal when needed, centered square cropping or transparent padding without stretching, one high-quality resize to the exact 512x512 master, alignment to the fixed ground-contact point, and zeroing RGB where alpha is zero; do not hand-paint, geometrically redraw, sharpen, recolor, or combine multiple candidates
- The 512x512 crop source is a lossless straight-alpha RGBA PNG with four transparent corners, zero hidden RGB, safe margins, and ground-contact point `(256, 465)`; above alpha `8`, its footprint stays inside x=`128..384`, its top lies between y=`210..300`, and its bottom lies between y=`450..475`, making it clearly smaller and younger than the unchanged mature crop while remaining readable
- Build `sunwheat_shadow_growing_512.png` deterministically in Pillow 12.2.0 from neutral cool gray RGB `(83, 86, 101)` and two maximum-alpha-merged components: a contact ellipse with pre-blur bounds x=`190..322`, y=`441..473`, peak alpha `66`, Gaussian blur radius `8`; and an attached subordinate cast ellipse with pre-blur bounds x=`224..344`, y=`447..477`, peak alpha `18`, Gaussian blur radius `12`
- The source shadow is a lossless straight-alpha 512x512 RGBA PNG with zero hidden RGB, transparent corners, continuously fading partial alpha, and shared ground-contact point `(256, 465)`; it begins behind the lowest leaves and its small down-right extension must not read as a separate oval, platform, puddle, or detached blob
- Create each 256x256 runtime file only by one Pillow 12.2.0 `Image.Resampling.LANCZOS` downsample directly from its corresponding accepted 512x512 source followed only by clearing RGB where alpha is zero; both runtime layers remain aligned on the full canvas and share exact fractional ground-contact point `(128, 232.5)` without independent repositioning or cropping
- Create complete `TF-ART-v1.1` metadata beside the crop source recording exact prompts, available generation identifiers, UTC date, candidate and preparation details, source/runtime hashes, dimensions, modes, alpha extrema and bounds, margins, transparent corners, hidden-RGB checks, palette and perspective review, shadow construction, crop/shadow overlap, pivot mapping, reviewer, date, and disposition
- The Coder report shows a temporary 512x256 comparison made from two independent 256x256 panels: each panel repeats the approved meadow tile 2x2 and places the approved empty plot at `(0, 0)`; the left panel then places the new growing shadow and crop at `(0, 0)`, while the right places the unchanged corrected ready shadow and mature crop at `(0, 0)`; do not add labels and do not commit the diagnostic
- At native size, the left panel must read as clearly growing and the right as clearly harvest-ready; both crops sit on the same plot without a ground-position jump, the growing crop remains readable without crowding the plot, and neither shadow creates a hover gap or detached gray shape
- All four Godot PNG import sidecars use lossless texture import with mipmap generation disabled, no size limit, no premultiplied alpha, and no normal-map, HDR, or channel-remap alteration
- No existing source or runtime asset is changed, and no scene, script, resource, autoload, project setting, audio file, balance file, style guide, or generation recipe references the new growing files yet
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully; boot prints `TOPFARMER_BOOT_OK`, and the balance test ends with `PASS: all 7 balance files loaded; bad test data was rejected`
Human check:
- Inspect the two-panel picture in the Coder's report at normal size. The left plot should show a small young green Sunwheat plant, while the right plot should show the approved full golden harvest-ready crop; the difference should be obvious without zooming
- Confirm the young plant still looks like Sunwheat because of its leaves and one small closed golden bud, but reject it if it already looks ripe, if the purple leaf becomes the main focus, or if it resembles a flowerpot, bouquet, or unrelated weed
- Compare where both plants meet the soil. They should sit at the same place on their plots, and the young plant's soft shadow should touch its lowest leaves without looking like a separate oval, puddle, or platform
- In Windows, right-click `sunwheat_crop_growing_512.png` and `sunwheat_shadow_growing_512.png`, choose Properties and then Details; both should report 512 by 512 pixels. Repeat for both files ending in `_256.png`; both should report 256 by 256 pixels
- Confirm the Coder report says the approved plot and mature Sunwheat files are unchanged and no game screen uses the new growing files yet
Depends on: TASK-013
Notes: see DEC-007, DEC-015, DEC-017, DEC-035, DEC-037, DEC-040, and DEC-042. This creates only the single growing visual used for the entire planted-but-not-ready interval in the first Phase 2 slice. Do not create seed, intermediate, wilted, night, or harvest animation states; do not create scenes, timers, interaction logic, camera behavior, UI layout, audio integration, or any other asset.
