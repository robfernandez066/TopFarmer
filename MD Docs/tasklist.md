TASK-026: Create a geometry-guided final-perspective Sunwheat candidate
Status: ready
Complexity: complex
Files: res://assets/source/ai/guides/sunwheat_ready_perspective_guide_512.png, res://assets/source/ai/candidates/sunwheat_crop_ready_geometry_candidate_512.png, their Godot import sidecars and metadata records, res://MD Docs/progress.md
Acceptance:
- Start from a clean `main` synchronized with `origin/main`. Do not modify, replace, rename, re-export, or integrate any existing source/runtime crop, shadow, plot, scene, script, test, style document, project setting, balance file, audio file, or localization file
- Create one non-runtime `512x512` transparent composition guide for a single upright crop. Mark exact ground contact `(256, 465)` and one continuous centered planted crown; divide the plant into larger/lower foreground leaf masses, overlapping middle grain-head masses, and smaller/higher rear grain-head masses so the intended near-to-far stack is unmistakable before generation
- The guide uses only simple labeled geometry, arrows, and flat diagnostic colors; it is not game art, contains no copied game imagery, and cannot be referenced by a Godot scene/resource. Its metadata explains every guide region and explicitly states that lower screen position means nearer/larger while higher means farther/smaller
- Generate the candidate using the guide as an actual visual reference, not merely a text description. Use the locked `TF-ART-v1.2` positive/negative prompt contract and record the exact prompt, negative prompt, referenced guide path, exposed generation identifier, and every permitted post-process operation
- The candidate is one compact mature Sunwheat plant with five to seven plump sun-gold grain heads, exactly one subordinate dusk-purple head, healthy green stalks/leaves, and all stems tapering or disappearing behind overlapping basal leaves into one organic planted crown at `(256, 465)`; it has no cut stalk ends, bundle tie, pot rim, multiple roots, multiple soil contacts, face, text, background, plot, or authored/baked cast shadow
- At both `512x512` and an external exact `256x256` reduction, the plant must read as a high-angle front-elevated upright crop: larger lower foreground leaves show short front planes, central heads overlap those leaves, smaller higher rear heads show modest top surfaces, and the silhouette has clear vertical depth. Reject straight-down radial rosettes/starbursts, eye-level bouquets, pure side views, isometric/diamond arrangements, disconnected head piles, or several separate plants
- Keep the planted crown visually centered on x=`256` rather than shifting the whole image to balance outer leaves or head luminance. If alignment correction is necessary, permit only one whole-image translation on the transparent canvas; do not crop, stretch, warp, mirror, rotate, hand-paint, recolor, composite candidates, or alter individual plant parts
- Deliver a lossless straight-alpha RGBA PNG at exactly `512x512`, with four transparent corners, no painted edge pixels, no hidden RGB beneath fully transparent pixels, and enough clear margin that the full silhouette remains uncropped. Use lossless Godot import with mipmaps off, no size limit, no premultiplied alpha, identity channels, and no normal-map/HDR conversion
- Create external diagnostics outside the repository: the candidate alone on neutral gray at native `512x512`, the guide overlaid at partial opacity on the candidate, and the candidate reduced exactly once to `256x256` and placed on the approved plot in a `540x960` farm-context image with its base at the existing soil contact. Do not create or commit a runtime crop or shadow in this task
- Run repository-isolation checks proving neither guide nor candidate is referenced by any scene, script, test, resource, autoload, or project setting. Run editor import, portrait display, FarmPlot visual-state, normal boot, and balance-loader regressions; all exit successfully with their established PASS or boot line
- The Coder report records file dimensions/hashes, alpha and edge checks, guide geometry, exact generation inputs/identifier, post-processing, planted-base measurement, perspective rejection checklist, external diagnostic paths, isolation results, regression results, and the Human's visual decision
Human check:
- Open the Coder's `540x960` farm-context image at normal size. Confirm the candidate looks like one crop growing upward from one natural base, not a bouquet, a flat flower shape viewed straight down, or several plants tied together
- Look for depth from bottom to top: the nearest leaves should be larger and lower, middle heads should overlap them, and rear heads should be smaller and higher with some top surface visible. The purple head should be only one small accent
- Open the guide-overlay image. Confirm the plant follows the guide's centered base and near/middle/far layers without being cut off or leaning visually to one side
- Tell the Coder `approved` only if all three checks pass. Approval permits a later task to create the separate shadow and runtime export; it does not integrate or replace the current placeholder yet
Depends on: TASK-025
Notes: see DEC-047 through DEC-054 and DEC-075. This resumes the required final Sunwheat replacement now that the functional Phase 2 plot loop and camera exist. TASK-025 pinch-feel refinement remains separately deferred under DEC-074 and must be specified and resolved before Phase 2 exit.
