TASK-024: Deploy the first playable farm build to a physical Android device
Status: ready
Complexity: complex
Files: append res://MD Docs/progress.md only; human creates ignored res://export_presets.cfg and machine-local Godot/Android settings; export temporary C:\tmp\TopFarmer-debug.apk outside the repository
Acceptance:
- Start from a clean `main` synchronized with `origin/main`. Do not change game code, scenes, tests, assets, balance data, localization, project settings, `.gitignore`, or any tracked file other than the append-only Coder report
- Human prerequisite: in Android Studio's SDK Manager, install or confirm Android SDK Platform 35, Android SDK Build-Tools 35.0.0, Android SDK Platform-Tools, and Android SDK Command-line Tools (latest) under `C:\Users\robbi\AppData\Local\Android\Sdk`. Do not download, install, upgrade, or remove machine software without explicit Human action or approval
- Confirm `java -version` and `javac -version` both report major version 17 and record the resolved JDK home. Confirm `C:\Users\robbi\AppData\Local\Android\Sdk\platform-tools\adb.exe version` succeeds, Platform 35 contains `android.jar`, Build-Tools 35.0.0 contains `apksigner`, and the latest command-line tools contain `sdkmanager.bat`
- Human prerequisite: in Godot Editor Settings, set the Android SDK and JDK paths to the verified machine locations. These are machine-local settings and must not create a tracked repository change
- Human prerequisite: create one local Godot export preset named exactly `Android`, mark it runnable, use package ID `com.robfernandez066.topfarmer`, app name `TopFarmer`, version code `1`, version name `0.1.0`, minimum SDK/API `29`, target SDK/API `35`, and enable only `arm64-v8a`. Use the installed Godot 4.7.1 Android export templates and debug signing only; do not create, request, expose, or commit release signing credentials
- Keep portrait orientation, Compatibility rendering, offline operation, and the existing 540x960 stretch contract. Do not enable internet-dependent features, permissions unrelated to Godot's basic launch requirements, expansion files, Gradle custom builds, plugins, XR, ads, payments, analytics, accounts, or cloud services
- Verify `export_presets.cfg` remains ignored and untracked. It may exist locally after Human configuration but must never be staged, committed, printed in full, or included in the Coder report because future presets can contain signing material
- Before export, run every existing required regression: editor import; portrait display; FarmPlot visual state, lifecycle, touch feedback, interaction controller, session UTC clock, and FarmWorld runtime tests; normal boot; and balance loader. All must exit successfully with their established PASS and boot lines
- Export exactly one debug APK with `godot_console --headless --path . --export-debug "Android" C:\tmp\TopFarmer-debug.apk`. The command exits successfully, the APK exists outside the repository, has nonzero size, and its SHA-256 and byte size are recorded without committing the APK
- Human prerequisite: connect one physical Android 10/API 29-or-newer ARM64 device by USB, enable Developer options and USB debugging, unlock it, and accept the computer's RSA debugging prompt. `C:\Users\robbi\AppData\Local\Android\Sdk\platform-tools\adb.exe devices -l` must show exactly one intended physical device in `device` state, not `unauthorized`, `offline`, or an emulator
- Install or replace the debug build only on that identified device using the explicit SDK ADB path and `adb install -r C:\tmp\TopFarmer-debug.apk`. Launch only package `com.robfernandez066.topfarmer` through its launcher intent; do not uninstall another package, wipe app data, reboot the device, change device settings beyond the Human prerequisites, or act on any other connected device
- Capture a bounded launch log proving the app starts without crash, ANR, renderer failure, missing-resource error, or script error and includes the existing `TOPFARMER_BOOT_OK` diagnostic. Do not capture or report unrelated device logs, account information, serial numbers, or personal data; redact the device serial in the Coder report
- With Wi-Fi and mobile data disabled by the Human, the installed build still launches to the complete four-plot portrait meadow, accepts touch, advances a planted Sunwheat from GROWING to READY on the real in-session clock, waits in READY, harvests on one tap, and smart-replants on the next tap without a network request or account prompt
- After device verification, `git status --short` is clean except the intended Coder report before its commit; no generated APK, Android build folder, local preset, keystore, SDK file, device identifier, signing material, or editor setting is tracked. Commit and push only the Coder report with the TASK-024 prefix
- The Coder report records tool versions and resolved paths, installed SDK component versions, preset name and nonsecret package/version/SDK/ABI fields, regression results, export command result, APK size/hash, redacted ADB state, install/launch result, bounded relevant log result, offline result, and the Human's physical touch findings
Human check:
- Install the missing Android SDK components in Android Studio and create the exact `Android` export preset described above when the Coder asks; connect and authorize one Android phone by USB
- After the Coder installs and launches the debug APK, hold the phone upright and confirm TopFarmer stays locked in portrait with four complete brown plots on meadow and no gray strip, clipping, crash, or account/network prompt
- Turn off both Wi-Fi and mobile data. Tap two different plots and confirm each gives one soft click and press response and grows independently
- Wait about two minutes for the first Sunwheat under the current CSV balance. Confirm it becomes READY without reopening the app, remains waiting, harvests to empty with one tap, and replants with the next single tap while the other plot remains unchanged
- Tell the Coder whether touch targets feel easy to hit, whether any double action occurs, and whether text, plots, or crops are too small on the physical screen. This task passes only after the Coder report records your physical-device result and the app remains usable offline
Depends on: TASK-023 and the Human-installed Android SDK/JDK 17/device prerequisites
Notes: see DEC-002, DEC-065 through DEC-068 and frozen K1, K2, K3, K5, and K6. This is the required first physical-device checkpoint immediately after the core plant/harvest loop became playable. Camera gestures, richer action feedback, inventory/rewards, save/offline elapsed, and final crop art remain later tasks.
