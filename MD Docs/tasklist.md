TASK-013: Generate the empty tilled-soil plot asset
Status: ready
Complexity: complex
Files: create res://assets/source/ai/production/tilled_soil_plot_empty_512.png and its Godot import sidecar; create res://assets/source/ai/production/tilled_soil_plot_empty_512.metadata.md; create res://assets/sprites/plots/tilled_soil_plot_empty_256.png and its Godot import sidecar
Acceptance:
- Use the built-in image-generation workflow and the locked `TF-ART-v1.1` recipe to create exactly one empty `tilled_soil` farm-plot prop; use this exact positive prompt: `TopFarmer game sprite of tilled_soil farm-plot prop with one screen-aligned rounded-square patch of warm rich brown earth, five broad shallow horizontal furrows, softly irregular moss-touched edges, a few sun-gold dry crumbs, and one tiny dusk-purple pebble, empty prepared plot ready for planting, 2D hand-painted fantasy cartoon, cozy rural warmth with a restrained mysterious undertone, mostly overhead top-down farm-game view with a slight downward angle, screen-aligned orthogonal ground plane, visible top surfaces and a small front face where the subject has height, readable mobile silhouette, TopFarmer locked v1 palette, key light from upper left at 35 degrees, subtle dark-plum outline, tile-filling opaque square canvas for terrain or isolated on transparent background for object sprites, centered on a 512 by 512 canvas, bottom-center ground contact aligned where applicable, no text`
- Use the locked negative prompt without additions or removals: `photorealism, 3D render, anime, pixel art, horror, gore, neon, glossy plastic, isometric projection, diamond tile, 30-degree isometric axes, diagonal grid movement, fisheye, front view, side view, inconsistent perspective, multiple objects, scenery, background, baked cast shadow, cropped edges, text, watermark`
- The accepted source shows one flat ground-level plot whose overall footprint reads as a rounded square with its four sides parallel to the canvas edges; it must not read as a diamond, diagonal tile, isometric rhombus, perspective trapezoid, oval mound, raised box, platform, hole, or puddle
- The plot uses soil `#7A4E3A` as the controlling color, a subtle outline derived from `#3E3840`, sparse edge touches derived from moss `#5E7A4A`, a few restrained dry crumbs derived from sun `#E9C46A`, and one tiny subordinate pebble derived from dusk `#705B8C`; value and saturation shifts may paint form, but no competing palette may dominate
- Five broad shallow furrows run left-to-right and remain screen-horizontal; they must be legible at 256x256 without forming text, a rune, a face, a grid, or diagonal grooves, and their internal shading may describe the soil but may not become a separate cast shadow
- Because the plot is flush with the ground, create no separate shadow sprite and accept no baked halo or down-right cast shadow; the color sprite must contain no crop, seed bag, plant, tool, character, building, button, scenery, writing, watermark, or background
- Prepare the accepted generated art only by lossless chroma/background removal when needed, centered square cropping or transparent padding without stretching, one high-quality resize to the exact 512x512 master, and zeroing RGB where alpha is zero; do not hand-paint, geometrically redraw, sharpen, recolor, or combine multiple candidates
- The 512x512 source is a lossless straight-alpha RGBA PNG with four transparent corners, zero hidden RGB, comfortable margins, a single centered footprint, and shared object ground-contact point `(256, 465)`; position the plot so its lower edge is near that point while retaining at least 16 transparent pixels on every canvas edge
- Create `tilled_soil_plot_empty_256.png` only by one Pillow 12.2.0 `Image.Resampling.LANCZOS` downsample directly from the accepted 512x512 source, followed only by clearing RGB where alpha is zero; the runtime file is a lossless straight-alpha RGBA PNG on an exact 256x256 canvas with ground-contact point `(128, 232.5)` and no crop, stretch, shift, repaint, or extra pixels
- Create complete `TF-ART-v1.1` metadata beside the source recording exact prompts, tool/model information exposed by the workflow, UTC generation date, candidate identifier, requested and delivered dimensions, preparation steps, source/runtime hashes, dimensions, modes, alpha extrema and bounds, margin and corner checks, hidden-RGB checks, palette review, perspective review, pivot mapping, reviewer, date, and disposition
- The Coder report shows two temporary native-size diagnostics: the approved runtime meadow repeated 2x2 with the empty plot at `(0, 0)`, and the same composition with the corrected Sunwheat shadow then crop layered at `(0, 0)` above the plot; do not commit either diagnostic
- In both diagnostics the plot lies flat on the meadow, remains visibly screen-aligned and square, and does not make the mature crop float; enough soil and furrow detail remains visible around the crop to identify the planted plot at default logical resolution
- Both Godot import sidecars use lossless texture import with mipmap generation disabled, no size limit, no premultiplied alpha, and no normal-map, HDR, or channel-remap alteration
- No existing source or runtime asset is changed, and no scene, script, resource, autoload, project setting, audio file, balance file, style guide, or generation recipe references the new plot yet
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully; boot prints `TOPFARMER_BOOT_OK`, and the balance test ends with `PASS: all 7 balance files loaded; bad test data was rejected`
Human check:
- Inspect both pictures in the Coder's report at normal size. In the empty version, the brown soil should look like one flat rounded-square plot laid directly on the grass, with five horizontal furrows; it must not look like a diamond, tilted tile, raised platform, hole, or puddle
- In the planted version, the approved Sunwheat should sit naturally on the soil with its soft shadow touching the lower leaves; reject it if the plot shape or shadow makes the crop look like it is floating
- Confirm the plot still has visible brown soil and furrow marks around the crop, and that the tiny purple pebble is only a quiet detail rather than the main thing you notice
- In Windows, right-click `tilled_soil_plot_empty_512.png`, choose Properties and then Details; it should report 512 by 512 pixels. Repeat for `tilled_soil_plot_empty_256.png`; it should report 256 by 256 pixels
- Confirm the Coder report says neither plot file is used by a game screen yet
Depends on: TASK-012
Notes: see DEC-007, DEC-017, DEC-031, and DEC-040. This creates and admits only the empty visual plot asset needed before the Phase 2 interaction slice. Do not create planted/growing crop states, plot scenes, interaction logic, timers, camera behavior, UI layout, audio integration, or any additional asset in this task.
