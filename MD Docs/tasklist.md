TASK-011: Correct the Sunwheat shadow and runtime pair
Status: ready
Complexity: complex
Files: preserve res://assets/source/ai/reference/sunwheat_crop_ready_512.png unchanged, replace res://assets/source/ai/reference/sunwheat_shadow_ready_512.png, update res://assets/source/ai/reference/sunwheat_crop_ready_512.metadata.md, create or preserve res://assets/sprites/crops/sunwheat_crop_ready_256.png and its Godot import sidecar, replace res://assets/sprites/crops/sunwheat_shadow_ready_256.png and its Godot import sidecar
Acceptance:
- The approved crop source remains byte-for-byte unchanged at SHA-256 `1979E084EA5929A510CA15095F814A191DBD0412C7CA2E9D40850D39DD9C25B6`; the runtime crop is reproduced or preserved byte-for-byte at the accepted local-export SHA-256 `711D3F1CBC73B9B6031795535CBAF723C6832C03A44EB1BE0DDF45F9156019F4`
- The rejected shadow source at SHA-256 `6150B4EEA02B3F24862657198F3E08B090346C30A7EB6C17BBCF15886CF38CF6` and rejected runtime shadow at SHA-256 `0DAC069A33F4B8EC2D1A4A0ED39539EDB5FFBDB9ECE0F01318E56FBA4A24725B` are recorded as superseded under DEC-036 and are not the final shadow files
- Rebuild the 512x512 shadow master deterministically in neutral cool gray RGB `(83, 86, 101)` with two attached partial-alpha components: a contact ellipse with pre-blur bounds x=`116..396`, y=`412..468`, peak alpha `78`, Gaussian blur radius `10`; and a subordinate cast ellipse with pre-blur bounds x=`184..404`, y=`426..470`, peak alpha `24`, Gaussian blur radius `14`
- Combine the two shadow alpha components by maximum alpha, keep every shadow pixel partially transparent, and clear RGB wherever alpha is zero; the contact component must overlap behind the crop's bottom leaves and ground point instead of beginning below them, while the cast component remains attached and shifts only modestly down and right
- The corrected 512x512 shadow master has fully transparent corners, a continuously fading edge, safe canvas margins, and no crop color, background, hard black edge, separate oval, platform, puddle, detached lobe, text, watermark, scenery, or extra object
- Produce `sunwheat_shadow_ready_256.png` only by one Pillow 12.2.0 `Image.Resampling.LANCZOS` downsample of the corrected 512x512 shadow master followed by clearing hidden RGB where alpha is zero; do not independently redraw or reposition the runtime shadow
- The runtime crop and corrected runtime shadow are lossless straight-alpha PNGs on exact 256x256 transparent canvases, retain the same full-canvas alignment, and share the fractional ground-contact point `(128, 232.5)` without moving, scaling, cropping, or regenerating the accepted crop
- The Coder report shows a temporary native-coordinate composite over the approved runtime meadow and a close view of the bottom leaves; the contact shadow must visibly begin underneath the leaf base with no hover gap, and the lighter attached extension must not read as a separate shape
- The Coder report records Pillow version, construction values, rejected and replacement shadow hashes, crop source/runtime hashes, dimensions, alpha extrema, occupied alpha bounds, hidden-RGB checks, shared ground point, and exact composite method
- Update `sunwheat_crop_ready_512.metadata.md` to mark the old source/runtime shadows Human-check rejected, cite DEC-036 and local commit `0eebb1d`, document the replacement-master construction and downsample method, record replacement hashes/bounds/alpha, and state that the crop source and accepted runtime crop are unchanged
- All involved Godot import sidecars use lossless texture import with mipmap generation disabled, no size limit, no premultiplied alpha, and no normal-map, HDR, or channel-remap alteration
- No other source or runtime asset is created or changed, and no scene, script, resource, autoload, project setting, or audio file references either runtime PNG yet
- The existing project boot and balance-loader checks still pass
Human check:
- In the Coder's report, inspect the full meadow composite and the close view under the bottom leaves; the crop must visibly rest on a soft shadow beginning beneath those leaves, with no clear gap or separate gray oval below it
- The shadow may extend gently toward the lower right, but reject it if it looks like a platform, puddle, detached blob, or broad offset shape that makes the crop hover
- Confirm the crop image itself is unchanged: it must remain the same approved low Sunwheat mound with golden heads, green leaves, and one muted purple-tinted head, without stretch, blur, recolor, or colored fringe
- In Windows, right-click `sunwheat_crop_ready_256.png` and `sunwheat_shadow_ready_256.png`, choose Properties, then Details; both should report 256 by 256 pixels
- Confirm the Coder report gives crop runtime SHA-256 `711D3F1CBC73B9B6031795535CBAF723C6832C03A44EB1BE0DDF45F9156019F4`, shared ground point `(128, 232.5)`, and no scene references
Depends on: TASK-010
Notes: see DEC-007, DEC-014, DEC-015, DEC-017, DEC-031, DEC-035, and DEC-036. The Human check overrides the earlier technical pass: correct pivot alignment is insufficient when the visible shadow shape makes the crop hover. Preserve the crop exactly, replace the shadow master and its runtime derivative, and do not create a committed diagnostic composite or begin plot, timer, interaction, camera, or UI work in this task.
