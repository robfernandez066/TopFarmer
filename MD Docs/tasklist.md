TASK-015: Create the reusable FarmPlot visual-state scene
Status: ready
Complexity: complex
Files: create res://scenes/farm/farm_plot.tscn; create res://scripts/farm/farm_plot.gd and its Godot UID sidecar; create res://tests/farm_plot_visual_state_test.gd and its Godot UID sidecar; create res://tests/visual/farm_plot_states_preview.tscn
Acceptance:
- Create `farm_plot.tscn` as a reusable `Node2D` scene with root node `FarmPlot` and exactly three direct `Sprite2D` visual children named `PlotBase`, `CropShadow`, and `CropColor`; do not add input, collision, timers, animation, UI, audio, particles, labels, or gameplay state
- Treat the root origin as the shared plot ground-contact point. Set all three sprites to `centered = false`, position each at exactly `(-128, -232.5)`, and keep their scale at `(1, 1)` so every approved 256x256 layer maps ground point `(128, 232.5)` to the root without rounding or independent offsets
- `PlotBase` always uses `res://assets/sprites/plots/tilled_soil_plot_empty_256.png` and remains visible in every state; set visual stacking to plot base below crop shadow below crop color, with no additional material, filtering override, modulation, rotation, skew, or scale change
- Attach `farm_plot.gd` and define exactly three visual states named `EMPTY`, `GROWING`, and `READY`; expose an editor-settable initial visual state defaulting to `EMPTY`, apply it on `_ready()`, provide `set_visual_state(state)` for changing it, and provide `get_visual_state()` for verification
- In `EMPTY`, hide `CropShadow` and `CropColor`. In `GROWING`, show them with `sunwheat_shadow_growing_256.png` and `sunwheat_crop_growing_256.png`. In `READY`, show them with `sunwheat_shadow_ready_256.png` and `sunwheat_crop_ready_256.png`. State changes must replace both textures together and never leave a growing/ready color-shadow mismatch
- Changing `EMPTY -> GROWING -> READY -> EMPTY` repeatedly keeps the same root position, sprite positions, scale, and base texture; no node is recreated, reparented, or moved during a state change
- Create `farm_plot_visual_state_test.gd` as a headless first-party test that instantiates `farm_plot.tscn`, verifies the exact node structure and transforms, verifies the default empty state, exercises the full state sequence twice, checks visibility and exact texture resource paths in every state, confirms the base remains unchanged, and ends with exactly `PASS: farm plot visual states` when successful
- Create `farm_plot_states_preview.tscn` only as a manual visual-check scene at the existing 540x960 logical portrait size. Fill its background by repeating only the approved 128x128 meadow tile and place three `FarmPlot` instances on the same vertical centerline with ground points at `(270, 290)`, `(270, 570)`, and `(270, 850)`
- Configure the preview's top instance as `EMPTY`, middle as `GROWING`, and bottom as `READY`; include no text, labels, frames, buttons, camera movement, interaction, extra art, or diagnostic overlays, and do not make the preview the project main scene
- At native logical size, all three plots are fully visible and screen-aligned, the growing and ready crops meet the soil at the same relative point, no crop or shadow jumps between states, and the three plot canvases do not overlap
- Do not modify any approved PNG, import sidecar, metadata, balance file, source-art rule, audio file, `project.godot`, `scenes/main.tscn`, `scripts/main.gd`, or existing test; do not reference source/master art from the scene
- `godot_console --headless --editor --path . --quit`, `godot_console --headless --path . --script res://tests/farm_plot_visual_state_test.gd`, `godot_console --headless --path . --quit-after 1`, and `godot_console --headless --path . --script res://tests/balance_loader_test.gd` all exit successfully; the new test ends with `PASS: farm plot visual states`, normal boot prints `TOPFARMER_BOOT_OK`, and the balance test ends with `PASS: all 7 balance files loaded; bad test data was rejected`
Human check:
- Open the project in Godot, find `tests/visual/farm_plot_states_preview.tscn` in the FileSystem panel, double-click it, and press F6 to run that scene
- A portrait meadow should appear with three separate plots stacked from top to bottom: an empty brown plot, a small young green Sunwheat plot, and a full golden harvest-ready Sunwheat plot
- Confirm all three plots are straight and screen-aligned, none overlap or clip off-screen, and the young and ready plants meet the soil in the same place without either shadow looking detached
- Nothing in this preview should respond when clicked or tapped; close it with F8 after checking
- Ask the Coder to demonstrate the headless test if desired; it should finish with `PASS: farm plot visual states`
Depends on: TASK-014
Notes: see DEC-017, DEC-037, DEC-040, DEC-042, DEC-043, and DEC-044. This task creates the visual-state component only. Do not implement planting, harvesting, seed selection, inventory, real-time growth, touch input, camera behavior, farm layout, HUD, sound, vibration, save data, or replace the main scene.
