TASK-028: Export the approved perspective Sunwheat runtime pair
Status: ready
Complexity: complex
Files: res://assets/source/ai/candidates/sunwheat_shadow_ready_geometry_candidate_512.png, its Godot import sidecar and metadata record, res://assets/sprites/crops/sunwheat_crop_ready_perspective_256.png, res://assets/sprites/crops/sunwheat_shadow_ready_perspective_256.png, their Godot import sidecars, res://MD Docs/progress.md
Acceptance:
- Start from a clean `main` synchronized with `origin/main`. Preserve the approved source crop `res://assets/source/ai/candidates/sunwheat_crop_ready_geometry_candidate_512.png` byte-for-byte at SHA-256 `6E0063C55F5E8CFA00F2011F39F1A496A70F57993EFC3CD914E7914556594B81`; do not alter or delete any existing source/runtime crop, shadow, guide, metadata, plot, scene, script, test, project setting, balance file, audio file, or localization file
- Create one separate deterministic straight-alpha `512x512` source shadow on transparent canvas using only RGB `(83, 86, 101)`: an alpha-72 contact ellipse with source bounding box `(204,443)..(308,469)` blurred by radius `8`, plus an attached alpha-24 down-right extension ellipse with bounding box `(214,444)..(338,480)` blurred by radius `12`, combined by per-pixel maximum alpha rather than addition
- Clear RGB wherever final alpha is zero. At alpha greater than `8`, the source shadow must be one connected shape and overlap the crop across at least `500` source pixels; the runtime pair must retain at least `100` overlapping pixels. The source shadow must also have four transparent corners, no painted edge pixel, and no detached lobe, broad offset oval, platform, puddle, halo, hard edge, or shadow painted into the crop itself
- Export `sunwheat_crop_ready_perspective_256.png` by one full-canvas Pillow 12.2.0 Lanczos reduction of the approved `512x512` crop to exactly `256x256`; export `sunwheat_shadow_ready_perspective_256.png` independently by one full-canvas Lanczos reduction of the new source shadow. Do not crop, stretch, translate, rotate, mirror, sharpen, recolor, composite, hand-paint, or resample either runtime file again
- Preserve the crop and shadow's shared source ground contact `(256,465)` as runtime point `(128,232.5)`. Both runtime layers must use identical full-canvas coordinates so later placement at `(-128,-232.5)` aligns the planted crown to the FarmPlot origin without centroid correction
- Both runtime PNGs are lossless straight-alpha RGBA at exactly `256x256`, have four transparent corners, no painted edge pixels, and zero hidden RGB. A fresh reconstruction from each committed source using the specified reduction must be pixel-identical to its committed runtime PNG
- Use lossless Godot imports for all three new PNGs with mipmaps off, size limit zero, no premultiplied alpha, identity channel mapping, and no normal-map/HDR conversion. Record exact source/runtime SHA-256 values, dimensions, alpha extrema/bounds, edge checks, connected-component count, crop-shadow overlap, and reconstruction equality
- Create external diagnostics outside the repository: the source crop and source shadow composited at native `512x512` on neutral gray; the runtime crop and runtime shadow composited at native `256x256` on neutral gray; and one `540x960` approved-meadow/plot context with the runtime pair placed at `(-128,-232.5)` relative to the plot origin. Do not integrate or reference the pair in any game scene, script, test, resource, autoload, or project setting in this task
- Run repository-isolation checks proving the new pair is unreferenced. Run editor import, portrait display, FarmPlot visual-state, lifecycle, touch-feedback, camera-controller, gesture-feedback, normal boot, and balance-loader regressions; all exit successfully with their established PASS or boot line
- The Coder report records the preserved approved-crop hash, exact shadow construction, source/runtime validation and hashes, import settings, external diagnostic paths, isolation results, regression results, clean tracked scope, and the Human's grounding decision
Human check:
- Open the Coder's `540x960` farm-context image at normal size. Confirm the new mature crop still has the approved angled depth and centered natural base, and that it sits on the soil rather than hovering
- Look closely at the base. The shadow should touch and partly disappear beneath the lowest leaves, with only a small soft extension down-right; reject it if it looks like a separate oval, puddle, platform, halo, or detached blob
- Open the native `256x256` neutral-background composite at 100%. Confirm the shadow is subtle enough that the crop remains the focus and that no part of the crop or shadow is clipped
- Tell the Coder `approved` only if all three checks pass. Approval permits a later task to replace the current READY placeholder in FarmPlot; this task itself must not change the game screen
Depends on: TASK-027
Notes: see DEC-054 through DEC-057, DEC-077, and DEC-079. This is a traceable final-perspective runtime export with a newly fitted shadow; the current placeholder remains live until a separate integration task and Human check.
