TASK-025: Add optional touch pan and pinch zoom to the playable farm
Status: ready
Complexity: complex
Files: res://scenes/farm/farm_world.tscn, res://scripts/farm/farm_world.gd, res://scripts/farm/farm_camera_controller.gd, res://tests/farm_camera_controller_test.gd, any required Godot .uid companions, res://MD Docs/progress.md; reuse the ignored Android preset and temporary C:\tmp\TopFarmer-debug.apk for the Human check
Acceptance:
- Start from a clean `main` synchronized with `origin/main`. Preserve all approved plot/crop assets, balance data, lifecycle timing, plot positions, touch sound, press scale, portrait settings, Android settings, and existing tests except for the minimum gesture-cancellation integration needed below
- Add exactly one reusable farm-camera controller owned by FarmWorld. It may control one Camera2D/view transform but must not own crop state, call the clock, choose crops, or perform plant/harvest actions
- The default camera is centered on farm point `(270, 480)` at zoom `1.0`, shows all four complete plots at once, and keeps EMPTY, GROWING, and READY silhouettes as legible as before; routine planting and harvesting require no pan or zoom
- Clamp zoom continuously to `1.0` through `2.0`. A two-finger pinch changes zoom proportionally to current finger separation and keeps the world point beneath the pinch midpoint visually stationary except where farm-edge clamping makes that impossible
- At zoom `1.0`, the farm remains centered and one-finger movement cannot displace it. Above `1.0`, one-finger movement beyond a `12` logical-pixel threshold pans the farm in the same direction as the finger with no inertia, fling, rotation, overscroll, bounce, or movement after release
- Clamp camera position after every pan or zoom so the existing meadow always covers the visible playfield and no renderer-clear color, gray strip, or out-of-farm area appears. Repeated movement against each edge must not accumulate drift or move past the same stable limit
- A release below the movement threshold with no second touch remains one normal FarmPlot tap and performs at most one existing gameplay action. Crossing the threshold or introducing a second touch cancels every pending plot activation for that gesture, restores any pressed plot to its exact normal transform, and cannot plant, harvest, or smart-replant
- Preserve TASK-020 feedback semantics: a plot where a gesture began may show its immediate approved press response and play its one approved press sound before the gesture is later classified as pan/pinch, but cancellation must restore it and must not play a duplicate sound or emit an activation
- Handle touch identifiers explicitly: a third or later finger cannot change the active pinch pair, activate a plot, or leave a stuck press; touch cancellation, focus loss, visibility loss, and scene exit restore stable input and camera state
- Provide editor-only mouse parity without changing touch authority: mouse wheel zooms in fixed `0.1` steps around the pointer within the same limits, middle-button drag pans with the same bounds, and ordinary left-click plot behavior remains unchanged
- Add a deterministic headless regression covering default framing, both zoom limits, pinch midpoint anchoring, pan threshold, all four camera edges, repeated clamp stability, tap-versus-drag-versus-pinch arbitration, extra-finger rejection, cancellation cleanup, mouse parity, no crop mutation during camera gestures, and one unchanged successful tap action. It exits successfully and prints exactly one final `FARM_CAMERA_CONTROLLER_TEST_PASS`
- Run editor import, portrait display, FarmPlot visual state, lifecycle, touch feedback, interaction controller, session UTC clock, FarmWorld runtime, new camera-controller, normal boot, and balance-loader regressions. Every command exits successfully with its established PASS or boot line and no unexpected error
- Re-export and install the debug APK only through the existing ignored `Android` preset and the same TASK-024 package. Keep the preset, stock Gradle output, APK, device identifier, and signing material ignored/untracked; do not change project/export configuration, permissions, SDK levels, ABI, signing, balance data, or network behavior
- The Coder report records the controller/scene contract, exact zoom and threshold values, regression results, APK hash/size, redacted install result, clean tracked-file scope, and the Human's physical-device findings
Human check:
- After the Coder installs the updated build on your phone, open TopFarmer upright. Before touching anything, confirm all four complete plots are visible and crops are as easy to read as before
- Place two fingers on the meadow and spread them. The farm should enlarge smoothly and stop enlarging at a sensible limit. Drag one finger while enlarged and confirm the farm follows your finger but never reveals a gray strip or empty area at any edge
- Pinch inward until it stops. The original full-farm view should return with all four plots visible and centered; dragging at this full view should not move the farm
- Start a drag or pinch on and around a plot several times. The plot may briefly press or click when your first finger lands, but camera gestures must never plant, harvest, or perform two actions, and no plot may remain shrunken after release
- Finally, tap one empty or ready plot normally. It should give one soft click and exactly one normal plant or harvest action. Tell the Coder whether pinch and pan feel responsive and whether any accidental plot action, stuck press, jitter, gray edge, or hard-to-read crop occurred
Depends on: TASK-024
Notes: see DEC-060 through DEC-067 and DEC-071 through DEC-072; implements the remaining C1/C2 camera contract while preserving C3 and H4 readability/touch behavior. Final Sunwheat perspective replacement remains required before Phase 2 exit under DEC-054.
